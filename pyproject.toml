[project]
name = "piighost"
version = "1.7.1"
description = "Protect personal data (PII) in your LLM prompts. piighost hides sensitive values from the model, then restores the real values in the response, so tools and the user still get the real data. Pluggable detectors (regex, NER, LLM), LangChain and Pydantic AI integrations, and a companion OpenAI/Anthropic proxy."
readme = "README.md"
license = "MIT"
license-files = ["LICENSE"]
authors = [
    { name = "Athroniaeth" },
]
keywords = [
    "pii",
    "anonymization",
    "de-identification",
    "pseudonymization",
    "nlp",
    "ner",
    "langchain",
    "langgraph",
    "gliner",
    "presidio",
    "spacy",
    "llm",
    "privacy",
    "ai",
    "agents",
]
classifiers = [
    "Development Status :: 5 - Production/Stable",
    "Intended Audience :: Developers",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
    "Topic :: Security",
    "Topic :: Text Processing :: Linguistic",
    "Typing :: Typed",
]
requires-python = ">=3.11"
dependencies = [
    "typing-extensions>=4.16.0",
]

[project.urls]
Homepage = "https://github.com/Athroniaeth/piighost"
Documentation = "https://athroniaeth.github.io/piighost/"
Repository = "https://github.com/Athroniaeth/piighost"
Issues = "https://github.com/Athroniaeth/piighost/issues"
Changelog = "https://github.com/Athroniaeth/piighost/blob/master/CHANGELOG.md"

[project.scripts]
piighost = "piighost.cli:main"

[project.optional-dependencies]
gliner2 = [
    # `[local]`, not the bare package: gliner2 2.0 became an API client and moved
    # torch, transformers, peft, numpy and safetensors behind that extra of its
    # own. Installing gliner2 alone therefore yields a Gliner2Detector that
    # raises ModuleNotFoundError on torch the first time it is built. Naming the
    # extra rather than the five packages keeps this tracking upstream.
    "gliner2[local]>=2",
]
redis = [
    "redis>=5",
]
langchain = [
    "langchain>=1.2",
]
# Alias of `langchain`, kept for back-compat. Prefer `piighost[langchain]`.
middleware = [
    "langchain>=1.2",
]
pydantic-ai = [
    "pydantic-ai>=2.0",
]
client = [
    "httpx>=0.28",
]
spacy = [
    "spacy>=3.7",
]
transformers = [
    "transformers>=4.40",
    "torch>=2.0",
]
llm = [
    "langchain-core>=0.3",
    "langchain>=1.2",
]
observation = [
    "opentelemetry-api>=1.30",
]
fuzzy = [
    "rapidfuzz>=3.9",
]
config = [
    "pydantic>=2.6",
    "pydantic-settings>=2.0",
    "typer>=0.12",
]
argon2 = [
    "argon2-cffi>=23.1",
]
crypto = [
    "cryptography>=44",
]
mistral = [
    "mistralai>=1.0",
]
presidio = [
    "presidio-analyzer>=2.2",
]
llama-index = [
    "llama-index-core>=0.14",
]
sqlalchemy = [
    "sqlalchemy[asyncio]>=2.0",
    "aiosqlite>=0.19",
    "asyncpg>=0.29",
]
all = [
    "piighost[gliner2,redis,langchain,middleware,pydantic-ai,client,spacy,transformers,llm,observation,fuzzy,config,argon2,crypto,mistral,sqlalchemy,presidio,llama-index]",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/piighost"]

[tool.hatch.build.targets.sdist]
# Ship only what a source install needs; keep docs/, site/, e2e/, benchmarks/,
# tools/, scripts/, publish/, and examples out of the source distribution.
only-include = [
    "src/piighost",
    "README.md",
    "LICENSE",
    "CHANGELOG.md",
]

[dependency-groups]
dev = [
    "ruff>=0.15.5",
    "pytest>=9.0.2",
    "pytest-cov>=6.0",
    "pyrefly>=0.55.0",
    "zensical>=0.0.27",
    "commitizen>=4.13.9",
    "pre-commit>=4.0",
    "pytest-asyncio>=0.25",
    "bandit>=1.9.4",
    "pydantic>=2.6",
    "pydantic-settings>=2.0",
    "typer>=0.12",
    "argon2-cffi>=23.1",
    "cryptography>=44",
    "redis>=5",
    "fakeredis>=2.20",
    "mistralai>=1.0",
    "langchain>=1.2",
    "pydantic-ai>=2.0",
    "opentelemetry-api>=1.30",
    "opentelemetry-sdk>=1.30",
    "rapidfuzz>=3.9",
    "sqlalchemy[asyncio]>=2.0",
    "aiosqlite>=0.19",
    "asyncpg>=0.29",
]
gliner2 = [
    "gliner2>=1.2.4",
    # See the [project.optional-dependencies] gliner2 note: peft is imported by
    # gliner2 2.0 but only declared under its extras.
    "peft>=0.10,<1",
]
presidio = [
    "presidio-analyzer>=2.2",
]
llama-index = [
    "llama-index-core>=0.14",
]
langchain = [
    "langchain>=1.2.13",
]
spacy = [
    "spacy>=3.7",
]
transformers = [
    "transformers>=4.40",
    "torch>=2.0",
]
llm = [
    "langchain-core>=0.3",
    "langchain>=1.2",
]
observation = [
    "opentelemetry-api>=1.30",
]
fuzzy = [
    "rapidfuzz>=3.9",
]
config = [
    "pydantic>=2.6",
    "pydantic-settings>=2.0",
    "typer>=0.12",
]

[tool.pytest.ini_options]
addopts = "-m 'not integration' -p no:anyio -p no:faker -p no:langsmith_plugin -p no:opik"
testpaths = ["tests"]
asyncio_mode = "auto"
# The observation conftest sets a global tracer provider for the whole session,
# so the clear-text tracing nudge fires for every pipeline built without a
# redactor. Silence that one message here; its own tests assert it explicitly.
filterwarnings = [
    "ignore:Observation traces carry clear text:UserWarning",
]
markers = [
    "integration: tests that load heavy optional dependencies (torch, transformers, gliner2, spacy). Deselected by default.",
]

[tool.coverage.report]
show_missing = true

[tool.ruff]
# tools/ holds self-contained dev scripts (the animation generator), not
# library code, so they are exempt from the strict annotation lint. docs/ and
# .claude/ are Markdown prose whose fenced Python blocks are hand-aligned for
# readability; ruff 0.16 reformats code in Markdown, which would flatten that
# alignment, so keep them out of ruff's scope.
extend-exclude = ["tools", "docs", ".claude"]

[tool.ruff.lint]
# Typing is mandatory: flake8-annotations (ANN) flags any missing argument or
# return annotation, in src and tests alike. ANN401 stays off, so an explicit
# Any is allowed where a type genuinely cannot be narrowed.
#
# Docstrings are mandatory too: pydocstyle (D) enforces the presence and the shape
# the code-style skill asks for on every module, class, function and constant.
# D403 (first word capitalised) is off because it contradicts that skill: a test
# docstring states the invariant starting with the identifier under test, in its
# real case ("end is the start offset plus the text length").
extend-select = ["ANN", "D"]
ignore = ["ANN401", "D403"]

[tool.ruff.lint.pydocstyle]
convention = "google"

[tool.ruff.lint.per-file-ignores]
# A Test* class is a grouping container the skill's test skeleton requires, not an
# API surface, so it carries no docstring of its own. The requirement holds for the
# test functions inside it, which D102 still enforces, and D101 stays on for src.
"tests/**" = ["D101"]

[tool.pyrefly]
# Scratch scripts under scripts/ reference removed APIs, and e2e/ scripts pull in
# server deps absent from the main venv, so both would show spurious errors.
project-excludes = [
    ".claude/**",
    "scripts/**",
    "tools/**",
    "benchmarks/**",
    "e2e/**",
    "dist/**",
    "site/**",
]
# Extra search paths for pyrefly:
#  - ``examples`` lets the detector example package resolve ``from .common``.
#  - the project root lets test files do ``from tests.conftest import ...``.
search-path = ["examples", "."]

[tool.bandit]
exclude_dirs = [
    "tests",
    "benchmarks",
    ".venv",
    "e2e",
    "site",
    "dist",
    "docs",
    ".idea",
    ".git",
    ".cache",
    ".pytest_cache",
    ".ruff_cache",
]
